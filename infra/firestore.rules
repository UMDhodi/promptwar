rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /venue/{doc} { allow read: if true; allow write: if false; }
    match /alerts/{doc} { allow read: if true; allow write: if false; }
    match /users/{uid} { allow read, write: if request.auth.uid == uid; }
  }
}
